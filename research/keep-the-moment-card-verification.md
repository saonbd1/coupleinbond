## Keep the Moment card verification

The homepage card now presents “Keep the moment.” as an on-chain keepsake explanation. The rendered card explains that minting writes the score to a blockchain, identifies Ink as the fast, low-cost home for the current mint flow, identifies Base as an Ethereum-connected path for wider reach, and states that a small network fee can apply before wallet confirmation.

The visual pass confirms that the card remains readable within the existing pink-to-lilac theme. The chain information appears in two compact panels, the gas note is visible, the `calculator.html#mintBtn` destination remains intact, and the card sits beside the other redesigned feature cards without a new image asset.

The static-site validators passed after the edit: 15-page content validation, 15-page SEO validation, and six blog-page schema validation all reported zero errors.
