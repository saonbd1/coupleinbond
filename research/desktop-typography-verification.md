## Desktop typography verification

The homepage now uses a larger desktop-only type scale for feature-card headings, body copy, action links, poll controls, the Keep the Moment card, and the Quote of the Day card. The shared footer also uses larger desktop sizes for navigation links, brand copy, the CTA, social label, and bottom bar text.

The mobile rules remain separate, so the existing compact mobile layout is preserved. The desktop browser preview rendered the larger type without breaking the three-column card grid or footer structure. The 15-page content and SEO validators passed with zero errors, and the source diff has no whitespace errors.
