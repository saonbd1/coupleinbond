# Featured Post Hero Verification

The homepage now places one `Featured Post of The Week` card inside the right-side hero note. It uses the deployable `assets/featured-post-week.jpg` image and links to `blog-posts/couple-bonding-activities-at-home.html`.

The previous duplicate featured-post card was removed from the lower feature grid. The homepage content validator passes, and the rendered DOM reports one hero featured card, one featured-post label, one local image source, and one article link.
