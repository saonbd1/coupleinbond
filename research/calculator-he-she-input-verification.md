# Calculator He and She Input Verification

The calculator labels now read **He** and **She**. The input values are empty on first load, while the example placeholders remain `Taylor` and `Jordan` and are visually softened with reduced opacity, italic styling, and a small blur filter.

The rendered DOM confirms centered input text, centered labels, both placeholders, and the existing Calculate Match button. The calculator scoring, wallet, and minting hooks remain unchanged.
