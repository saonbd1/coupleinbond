## Footer label, alignment, and font-size verification

The shared footer renderer contains no “Explore” or “More to explore” labels. The footer keeps five links in each navigation column and centers both columns on desktop and mobile layouts.

Footer navigation links now use a larger `0.9rem` size with centered text and an 11px vertical gap. The brand, supporting copy, CTA, social label, and bottom bar text also use slightly larger sizes for improved readability. The centered brand and mobile stacking remain intact.

The homepage browser preview rendered the centered footer successfully. The footer source check found no Explore labels, and the 15-page content and SEO validators passed with zero errors.
